/**
 * Internal profile-storage schema marker.
 *
 * This is intentionally not exposed in the UI. Keeping the value stable
 * preserves existing local profiles while allowing future migrations.
 */
export const PROFILE_SCHEMA_VERSION = 'v1.5';
